name: Day 8 - Debugging

on:
  push:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show environment
        run: env

      - name: Show GitHub context
        run: echo "${{ toJson(github) }}"
